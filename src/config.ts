export type ItemProps = {
    index: number;
    name: {
        common: string;
    }
}

export const noResults = {
    name: {
      common: 'No Results found.'
    }
  }
  
export const loading = {
    name: {
      common: 'Loading...'
    }
  }